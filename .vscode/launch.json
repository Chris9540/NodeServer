{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Start",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Start - Clean",
            "request": "launch",
            "command": "npm run start clean",
            "cwd": "${workspaceFolder}"
        }
    ]
}